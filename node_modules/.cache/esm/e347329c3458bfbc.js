let express,APP_PORT,routes;_345‍.w("express",[["default",["express"],function(v){express=v}]]);_345‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);_345‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);

const app= express();
 //as we have exported routers using default earlier, so we can give it any name, as here we give routes 

app.use('/api', routes); //our all routes inside routes folder in index.js will get registered in our app using app.use().. And all routes starts after /api/... 


app.listen(APP_PORT, ()=> console.log(`Listening on port ${APP_PORT}`))